language: php

cache:
    directories:
    - $HOME/.composer/cache/files

matrix:
    fast_finish: true
    include:
        - php: '7.2'
          env: SNIFF=1
        - php: '7.3'
        - php: 'nightly'

    allow_failures:
        - php: nightly